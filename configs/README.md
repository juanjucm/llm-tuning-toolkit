# Configs

Reusable benchmark definitions live here.

```text
benchmarks/<model>/<instance>/bench_config.yaml
auto-tune/<model>/<instance>/<scenario>.yaml
```

Generated result folders and Parquet files are local outputs and ignored by git. Run folders may contain copied configs as provenance, but source configs should start here.
